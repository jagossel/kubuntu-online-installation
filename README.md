# kubuntu-online-installation

Script to automatically install all of the software on a Kubuntu 24.04 LTS
instance.

## To execute the script:

```bash
sudo bash <path to script>/install-software.sh
```
